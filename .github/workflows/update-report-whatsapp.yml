name: Update report WhatsApp

on:
  push:
    paths:
      - 'scripts/update-report-whatsapp.py'
      - '.github/workflows/update-report-whatsapp.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/update-report-whatsapp.py
      - run: |
          grep -q '77051871335' index.html
          ! grep -q '77072080553' index.html
          grep -q 'aa-kaz-v9' sw.js
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html sw.js
          git diff --cached --quiet || git commit -m "Correct report WhatsApp number"
          git push
